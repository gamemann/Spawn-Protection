{
    "version": "2.0.0",
    "options": 
    {
        "env": 
        {
            "sm_path": "/home/dev/sourcemod/scripting"
        }
    },
    "tasks": 
    [
        {
            "label": "sourcepawn",
            "type": "shell",
            "command": "$sm_path/spcomp64 ${file} -D ${workspaceFolder} -i ${workspaceFolder}/scripting/include -i $sm_path/include"
        }
    ]
}